<%# Step 1 — prepend new post to list %>
<%= turbo_stream.prepend "posts_list",
    partial: "post",
    locals: {post: @post} %>

<%# Step 2 — reset the form %>
<%= turbo_stream.update "post_form",
    partial: "form",
    locals: { post: Post.new } %>

